from setuptools import find_packages, setup

setup(
    name='aiohttp_plugin',
    version='0.0.1',
    packages=find_packages(),
)

setup(
    name='p3back',
    version='0.0.1',
    packages=find_packages(),
)
